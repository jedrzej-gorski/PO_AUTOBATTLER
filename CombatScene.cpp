#include "CombatScene.h"

void startCombat() {
	window.clear();

	for (int i = 0; i < MAX_TEAM_SIZE; i++) {
		if (playerTeam[i].getUnitType() != "NULL") {

		}
		//suggestion: use shop's random unit generator to generate enemy team
	}
}

void resolveEventQueue() {
	int currentHealth[2];
	if (playerTeam[0].getUnitType() != "NULL") && enemyTeam[0].getUnitType() != "NULL"){
		currentHealth[0]=playerTeam[0].
		currentHealth[1]=
	}

	while(playerTeam[0].)
}